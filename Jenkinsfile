pipeline {
    agent any
    environment {
      PATH = "$PATH:/opt/apache-maven-3.8.8/bin"
    }
    
    stages {

        stage('CLEAN WORKSPACE') {
            steps {
                cleanWs()
            }
        }

     

      
        
    }
}
